#include "pch.h"
#include <sstream>
#include <iostream>

#include "../Frosh-defence/frosh-defence/Frec.h"

/*TEST(FrecTest, All) {
	Frec zero = Frec{};
	Frec one = Frec{};
	Frec two = Frec{};
	Frec three = Frec{};
	Frec four = Frec{};
	EXPECT_EQ(0, zero.getID());
	EXPECT_EQ(1, one.getID());
	EXPECT_EQ(2, two.getID());
	EXPECT_EQ(3, three.getID());
	EXPECT_EQ(4, four.getID());
}*/